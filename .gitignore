# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js / TypeScript (artefatos gerados)
.next/
out/
build/
dist/
tsconfig.tsbuildinfo
*.tsbuildinfo

# Environment / secrets - NUNCA versionar credenciais
.env
.env.local
.env.development
.env.development.local
.env.test.local
.env.production
.env.production.local
.env*.local

# Chaves e certificados
*.pem
*.key
*.crt
*.p12
*.pfx
secrets/
credentials*.json
*token*.json

# Config local do Drizzle contem credencial do banco local - nao versionar
# (use drizzle.config.example.json como modelo)
drizzle.config.json

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editores
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vscode/*
!.vscode/extensions.json
.idea/

# Testes / cobertura
coverage/
.nyc_output/

# Vercel / Turbo
.vercel
.turbo

# Drizzle local (mantem config de exemplo no repo, mas nada sensivel)
drizzle/*.db
*.sqlite
*.sqlite-journal

# Backups locais do banco - NUNCA versionar (contem dados e hashes)
backup.sql
backup-*.sql
*.backup.sql
